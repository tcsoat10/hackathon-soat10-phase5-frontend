# ============================================
# EXEMPLO DE CONFIGURAÇÃO DE AMBIENTE
# ============================================
# Copie este arquivo para .env.local para desenvolvimento local
# NÃO comite arquivos .env com dados sensíveis!

# ============================================
# DESENVOLVIMENTO LOCAL
# ============================================
# Use VITE_API_BASE_URL=/ para ativar proxy do Vite em desenvolvimento
VITE_API_BASE_URL=/

# URL do backend que o proxy do Vite usará em desenvolvimento
VITE_DEV_PROXY_TARGET=http://localhost:8000

# ============================================
# PRODUÇÃO (Configure no Vercel Dashboard)
# ============================================
# VITE_API_BASE_URL=/
# BACKEND_URL=https://api.seudominio.com

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. Em DESENVOLVIMENTO:
#    - Use VITE_API_BASE_URL=/ (proxy do Vite ativo)
#    - Configure VITE_DEV_PROXY_TARGET para seu backend local
#
# 2. Em PRODUÇÃO (Vercel):
#    - Configure VITE_API_BASE_URL=/ (usa proxy do Vercel)
#    - Configure BACKEND_URL com a URL completa do seu backend
#    - Exemplo: BACKEND_URL=http://api.seudominio.com
#    - Ou se tiver HTTPS: BACKEND_URL=https://api.seudominio.com
#
# 3. VARIÁVEIS VITE_* são embutidas no build (client-side)
# 4. BACKEND_URL é usada apenas no servidor (função proxy do Vercel)
