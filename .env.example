# Exemplo de .env — copie para .env.local e ajuste localmente
# Em dev: use VITE_API_BASE_URL=/ para ativar proxy do Vite
VITE_API_BASE_URL=/

# Target absoluto que o proxy do Vite usará em desenvolvimento
VITE_DEV_PROXY_TARGET=http://localhost:8000

# Em produção, substitua por uma URL absoluta
# VITE_API_BASE_URL=https://api.seudominio.com
